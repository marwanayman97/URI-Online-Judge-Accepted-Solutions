#include <iostream>
#include <iomanip>

using namespace std;

int main() {


     int hrs,avg_speed;
     cout << fixed << setprecision(3);
     cin>>hrs>>avg_speed;
     cout<< (hrs * (double)avg_speed)/12 << endl;

    return 0;
}
