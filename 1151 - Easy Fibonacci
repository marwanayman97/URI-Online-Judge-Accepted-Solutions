#include <iostream>


using namespace std;

int main(){
           
    int N,a=0,b=1,count=0,old_a,old_b;
         
    cin>>N;
         
    if(N>0 && N<46){
         
      while (count < N){
	 	old_a = a;
        old_b = b;
        a = old_b;
        b = old_a + old_b;
        count++;
        
             if(count==(N)){
                           	cout<<old_a<<endl;
	                       }else{
	
                           cout <<old_a <<" ";
                                }
                       }
    
                        }
		   
      
    return 0;
  }
